# finbert-sentiment-analyzer-api
This contains the ML backend and REST API powering the Financial News Sentiment Analyzer I developed. It is built using FastAPI and PyTorch, and it returns sentiment predictions as "Bullish", "Bearish", or "Neutral" based on the input text data. It logs real-time predictions using a Postgres DB with SQLAlchemy.
